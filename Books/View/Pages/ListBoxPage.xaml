<Page x:Class="Books.View.Pages.ListBoxPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Books.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ListBoxPage" Loaded="Page_Loaded">

    <Grid>
        <ListBox Name="BooksLb">
            <ListBox.ItemsPanelTemplate>
                
            </ListBox.ItemsPanelTemplate>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="10" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{Binding Rank}" />
                        <TextBlock Grid.Row="2" Text="{Binding Title}" />
                        <TextBlock Grid.Row="4" Text="{Binding Author}" />
                        <TextBlock Grid.Row="6" Text="{Binding Published}" />
                        <TextBlock Grid.Row="8" Text="{Binding Country}" />
                        <TextBlock Grid.Row="10" Text="{Binding OriginalLanguage}" />
                        <TextBlock Grid.Row="12" Text="{Binding PageRange}" />
                        <TextBlock Grid.Row="14" Text="{Binding WordCount}" />
                        <TextBlock Grid.Row="16" Text="{Binding ID}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
